Bhai ye app ka code direct GitHub repo me hi likhna hai or me token de rha ho clasic usse hi me GitHub connect nhi karunga to uska wait mat karna

Ye token GITHUB_PERSONAL_ACCESS_TOKEN

You are a senior Flutter + Supabase + Python architect. Build a production-grade, industrial-level personal trading app called "GoldScalper Pro – XAUUSD Paper Trading Bot Platform".

PROJECT GOAL: Build a fully offline-capable Android APK (personal use only) for paper trading XAUUSD (Gold) with live candles from 100% FREE APIs. Users can upload custom scalping bot logic (Python or JSON strategy), run multiple bots 24/7 in pure demo/paper mode. No real money, no broker connection. App must run at $0/month cost. Code must be clean, modular, highly documented, GitHub-ready, with auto signed APK build on every push to main.

TECH STACK (MANDATORY – Do not change):

Frontend: Flutter 3.22+ (Dart), Material 3 Design, Riverpod for state management. Support offline mode with local SQLite (drift package) cache + Supabase sync.
Charting: Use lightweight_charts package version ^0.0.8 OR candlesticks package. Do NOT use tradingview_lightweight_charts (wrong name). Full TradingView-style interactive chart with M1/M5/M15/M30/H1, volume, zoom, crosshair, live price line.
Backend & DB: Supabase (Postgres + Auth + Realtime + Storage + Edge Functions + pg_cron).
Data Source (Priority order for ZERO COST):
PRIMARY (100% FREE, NO API KEY, NO LIMIT): Dukascopy free tick data via Python library dukascopy-node or dukascopy-python. Fetch XAUUSD live ticks and aggregate to 1m candles locally. This is unlimited.
FALLBACK 1: Binance PAXG/USDT public API (free, unlimited) - https://api.binance.com/api/v3/klines?symbol=PAXGUSDT&interval=1m
FALLBACK 2: TwelveData API (free tier 800/day) → https://api.twelvedata.com/time_series?symbol=XAU/USD&interval=1min&apikey=KEY - use only if primary fails. Support API key rotation via env array TWELVEDATA_KEYS.
FALLBACK 3: Finnhub free tier.
Bot Engine: Primary = Supabase Edge Functions (Deno) only for live bot execution to keep cost $0. Secondary = Python FastAPI microservice on Fly.io Mumbai region (bom) free tier ONLY for heavy backtests. Do not use both for same task. Live loop must run on Edge Function.
CI/CD: GitHub Actions → auto build signed APK on push to main + create GitHub Release. Handle keystore automatically: If ANDROID_KEYSTORE_BASE64 secret exists, use it. If not, generate debug keystore automatically with keytool -genkey -v -keystore debug.jks -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Debug" so build never fails.
DATABASE SCHEMA (Supabase SQL migration – complete & production ready – create file supabase/migrations/001_initial.sql):

profiles (id uuid PK references auth.users, email text, display_name text, preferred_language text default 'en', created_at timestamptz, updated_at timestamptz)
candles (id bigserial, symbol text default 'XAU/USD', timeframe text default '1m', open float8, high float8, low float8, close float8, volume float8, timestamp timestamptz UNIQUE, created_at timestamptz) - IMPORTANT: Add retention policy - keep only last 30 days M1, auto-aggregate older data to M15 to stay under 500MB free limit.
bots (id uuid PK default gen_random_uuid(), user_id uuid references profiles(id), name text, description text, code_content text, language text check (language in ('python','json_strategy')), version text default '1.0', is_active bool default false, parameters jsonb default '{}', magic_number int, created_at, updated_at)
paper_accounts (user_id uuid PK references profiles(id), balance float8 default 10000, equity float8 default 10000, initial_balance float8 default 10000, currency text default 'USD', max_daily_loss float8 default 500, max_lot_size float8 default 1.0, updated_at)
paper_trades (id uuid PK default gen_random_uuid(), user_id uuid, bot_id uuid references bots(id), symbol text, side text check (side in ('BUY','SELL')), entry_price float8, exit_price float8, sl float8, tp float8, lot_size float8, pnl float8, commission float8 default 0, status text check (status in ('OPEN','CLOSED','CANCELLED')), opened_at timestamptz, closed_at timestamptz, magic_number int, comment text)
bot_logs (id bigserial, bot_id uuid, level text check (level in ('INFO','WARN','ERROR','SIGNAL')), message text, candle_timestamp timestamptz, created_at default now())
user_settings (user_id uuid PK references profiles(id), risk_percent float8 default 1.0, max_open_trades int default 3, enable_news_filter bool default false, trading_sessions jsonb default '{"london": true, "newyork": true, "asian": false}', notification_prefs jsonb) Enable RLS on all tables with policies: users can only access own rows. Enable Realtime on candles, paper_trades, paper_accounts, bots. Create indexes: create index idx_candles_symbol_time on candles(symbol, timeframe, timestamp desc); create index idx_trades_user_status on paper_trades(user_id, status);
CORE FEATURES:

A. Live Data Ingestion Service (ZERO COST DESIGN):

Supabase Edge Function fetch-xau-candles triggered by pg_cron every 60 seconds.
Logic: Try Dukascopy python service first (hosted on Fly.io free). That service fetches live tick and returns aggregated 1m candle JSON. If fails, try Binance PAXGUSDT, if fails then try TwelveData with key rotation.
Insert with ON CONFLICT (timestamp) DO NOTHING.
Also create function aggregate-timeframes that aggregates M1 to M5/M15/M30/H1 every 5 minutes to save DB space.
Error logging to bot_logs table.
B. Flutter App Screens (Material 3 + Dark/Light theme + Hindi/English switch like MT5):

Splash + Auth (Supabase Auth – email/password + magic link). First-time setup: create paper_account with $10,000 via trigger.
Dashboard: Paper balance, equity, floating P&L, daily P&L, winrate, profit factor, open positions list, active bots status, equity curve mini chart (last 7/30 days) using fl_chart.
Live Chart Screen (full screen, landscape support): lightweight_charts candlestick + volume, Realtime subscription to candles table, Timeframe switcher M1/M5/M15/M30/H1, live bid/ask + simulated spread 0.30–0.50, indicators EMA 9/21/50, RSI(14), ATR(14), Bollinger Bands, Drawing tools (horizontal lines) saved in local Hive.
My Bots Screen: List + upload .py/.json, code preview with flutter_code_editor + syntax highlight, editable parameters panel (lot size, SL, TP, EMA periods, RSI levels, risk %, session filters, magic number) stored in bots.parameters jsonb, Activate/Deactivate toggle, Version history, Realtime sync.
Backtest Screen: Select bot + date range + timeframe, call Fly.io Python service /backtest endpoint which uses Dukascopy historical data (free), show results total trades, winrate, profit factor, max drawdown, Sharpe, equity curve chart, trade list, export PDF/CSV.
Trade History + Analytics + Settings (API keys encrypted, risk management, session filters, language EN/HI, theme).
C. Bot Execution Engine (CRITICAL – Fully Sandboxed – MUST RUN ON SUPABASE EDGE FUNCTION FOR $0 COST):

Edge Function run-active-bots triggered by pg_cron every 60 seconds on candle close.
Steps: Fetch latest closed candle + calculate indicators locally (EMA, RSI, ATR, BB using pure JS/TS functions, no external lib), Load all bots where is_active=true, For each bot: execute logic in isolated VM (Deno - no os, no network), Bot must follow strict interface: function on_candle(candle: {open,high,low,close,volume,timestamp}, indicators: {ema9,ema21,rsi,atr,bb_upper,bb_lower}, position: {side,entry_price,pnl} | null, params: object) => {action: "BUY"|"SELL"|"HOLD"|"CLOSE", sl?: number, tp?: number, lot?: number, comment?: string}
If BUY/SELL -> create paper_trade with spread random 0.30-0.50 + slippage 0.05-0.15, update paper_accounts.
Support trailing stop, break-even, partial close 50% at 1R.
Log every decision to bot_logs.
D. Built-in Example Bots (must ship with app + in supabase/seed.sql):

EMA Cross Scalper V3 (9/21 + ATR filter) - most stable
RSI + Bollinger Scalper V3
SNR Momentum / ICT Lite (Support-Resistance + FVG detection)
M1 High-Frequency Scalper (engulfing + momentum)
JSON Strategy template: { "rules": [{"if": "rsi < 30 and close < bb_lower", "action": "BUY", "sl": "atr1.0", "tp": "atr1.5"}] }
E. Project Structure (Monorepo): /app (Flutter) /supabase/functions/fetch-xau-candles, run-active-bots, aggregate-timeframes /supabase/migrations/001_initial.sql /engine (Python FastAPI - ONLY for backtest and Dukascopy live proxy) with main.py, requirements.txt, Dockerfile, fly.toml (region bom, min_machines_running 0 to stay free) /.github/workflows/build-apk.yml (auto build with debug keystore fallback) /README.md with $0 deploy guide /.env.example

F. README Must Include $0 Cost Deployment Guide:

How to get Supabase free project
How to deploy Edge Functions
How to deploy Python engine to Fly.io free tier with fly launch --region bom
How to setup GitHub Secrets (optional)
How Supabase 30-day retention keeps DB under 500MB free limit
Start implementation order:

Create supabase/migrations/001_initial.sql with all tables + RLS + indexes + retention
Create Flutter skeleton with Riverpod + Supabase init + offline Drift DB
Create Edge Function fetch-xau-candles with Dukascopy primary logic
Create Edge Function run-active-bots with sandboxed execution
Create Python FastAPI engine with /live-candle (Dukascopy proxy) and /backtest endpoints
Build all Flutter screens
Create GitHub Actions workflow with debug keystore fallback
Seed example bots
Build it production-ready, well documented, no placeholders. G. Extra Production Requirements:

Proper error handling + Sentry or Supabase logging
Rate limit protection on all APIs
Bot code versioning + diff view
One-click “Share Bot” (export .py + params as zip)
Push notifications via Firebase or Supabase
Accessibility + performance optimized for mid-range Android devices
Comprehensive unit + integration tests for bot sandbox and paper engine
Deliverables:

Complete working monorepo
SQL migrations ready to run
Example bots with full documentation
GitHub Actions that builds signed APK
Fly.io deployment scripts
Detailed README with local setup + production deployment steps
Start by creating the full folder structure, database migration, and core Flutter skeleton with Riverpod + Supabase integration.

